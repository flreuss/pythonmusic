from pythonmusic.io.ir import IrNode, IrChannel, IrFile
from pythonmusic.io.midi_message import MidiMessage


def nodes_to_midi(nodes: list[IrNode], mspb: int) -> list[MidiMessage]:
    output = []

    for node in nodes:
        pass

    return output
