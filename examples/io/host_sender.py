"""
# Create a Midi Sender Object

This example shows how to send midi messages from a midi port.

# Usage
This script opens a midi port that should be visible from other applications. For
instance, DAWs can use the object created below as a virtual midi input source.

Alternatively, this module also provides other example scripts that can be used
to receive midi messages. See `attach_receiver.py`.

# Dependencies
Make sure that python packages `python-rtmidi` and `mido` are installed in your 
virtual environment.
"""

# from pythonmusic
